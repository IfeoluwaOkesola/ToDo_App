export declare const config: () => {
    PORT: {
        APP_PORT: string;
    };
    JWT_SECRET: string;
    JWT_EXPIRES: string;
    database: {
        type: string;
        url: string;
    };
};
